{
  "name": "creatorgraph",
  "version": "1.2.0",
  "private": true,
  "description": "Discover your Twitter tribes and network intelligence — client-side graph analysis",
  "author": "Shango Bashi",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
